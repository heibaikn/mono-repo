import { createApp } from 'vue'
import App from './App.vue'
// import formV3 from '@heibaimono/form-v3'
const app = createApp(App)
// app.use(formV3)
// console.log(draggable);
// app.component('draggable', draggable)
app.mount('#design-app')
